type Membership implements Node{
  id: ID! @id
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
  title: String!
  description: String!
  genere: String!
  points: Int!
  type: TypeMembership!
}

enum TypeMembership {
  FAN
  SUPERFAN
  DIOS
}